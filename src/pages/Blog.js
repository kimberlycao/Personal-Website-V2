import BlogsList from "../components/BlogsList";

const Blog = () => {
  return (
    <div className="min-h-screen bg-my-gray flex justify-center items-start">
      <BlogsList />
    </div>
  );
};

export default Blog;
